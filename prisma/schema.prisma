// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Players {
  id         String   @id @default(uuid())
  nome       String
  idade      Decimal
   
  @@map("jogadores")
}

model Teams {
  id         String   @id @default(uuid())
  nome       String
  fundacao   DateTime 
   
  @@map("times")
}

model Championships{
 id          String   @id @default(uuid())
 nome        String
 dataInicio  DateTime 
 dataFim     DateTime 
 Time        Teams?    @relation(fields: [timeId], references: [id])
 timeId      Int?
   
  @@map("campeonatos") 
}